diff --git a/RetreatUI/CHANGELOG.txt b/RetreatUI/CHANGELOG.txt
index 3aa32f3..ffc785c 100644
--- a/RetreatUI/CHANGELOG.txt
+++ b/RetreatUI/CHANGELOG.txt
@@ -1,3 +1,14 @@
+RetreatUI v1.0.11-beta.5
+============================
+
+ELVUI RIGHT CHAT + CLASS FONT COLOR TEST
+- Preserves chat windows and tabs placed in ElvUI's right chat panel.
+- Keeps the right-panel Data Text strip untouched.
+- Continues removing only standalone Loot/Trade windows outside the right panel.
+- Uses the active RetreatUI class accent as the default ElvUI value/font color.
+- Migrates the legacy Knight-red default without overwriting custom colors.
+- Includes all Witch Doctor Voodoo tracking from beta.4.
+
 RetreatUI v1.0.11-beta.4
 ============================
 
 diff --git a/RetreatUI/Core/Version.lua b/RetreatUI/Core/Version.lua
index 130efd2..cbfd6aa 100644
--- a/RetreatUI/Core/Version.lua
+++ b/RetreatUI/Core/Version.lua
@@ -2,16 +2,14 @@ local RUI = RetreatUI
 
 RUI.changelog = {
   version = RUI.version,
-  title = "RetreatUI v1.0.11-beta.4",
-  summary = "Witch Doctor Voodoo tracking test: Malice, Threads, Loa state, wards and missing utility.",
+  title = "RetreatUI v1.0.11-beta.5",
+  summary = "Right chat panel preservation and class-colored ElvUI font defaults.",
   changes = {
-    "Added Hex of Malice / Zalazane's Malice target tracking with expiry warnings.",
-    "Moved Puppeteer's Threads to the target-debuff tracker.",
-    "Added a dedicated active Loa state and stack display.",
-    "Added Drakkari Barrier cooldown and active-duration tracking.",
-    "Added Voodoo Strings, Shadowflare and Voodoo Idol coverage.",
-    "Added Healing Ward and Cleansing Idol utility tracking.",
-    "Kept uncertain Ascension names learned-only or active-only for safe live testing.",
+    "Chat windows and tabs placed inside ElvUI's right chat panel are now preserved.",
+    "Standalone Loot/Trade windows can still be cleaned up without touching right-panel content.",
+    "ElvUI's default value/font accent now follows the active RetreatUI class theme.",
+    "Legacy Knight-red value colors migrate once while custom user-selected colors remain untouched.",
+    "Retained all Witch Doctor Voodoo tracking from beta.4.",
   },
 }
 local updateFrame
 diff --git a/RetreatUI/Integrations/ElvUI.lua b/RetreatUI/Integrations/ElvUI.lua
index 2740d9c..8025f60 100644
--- a/RetreatUI/Integrations/ElvUI.lua
+++ b/RetreatUI/Integrations/ElvUI.lua
@@ -111,17 +111,75 @@ function RUI:AreElvUINamePlatesDisabled()
     and db.integrations.elvui.nameplateAddonDisableOK ~= false
 end
 
+local function ObjectName(value)
+  if not value or type(value.GetName) ~= "function" then return nil end
+  local ok, name = pcall(value.GetName, value)
+  return ok and type(name) == "string" and name or nil
+end
+
+local function IsRightChatNamed(value)
+  local name = ObjectName(value)
+  return name and string.find(string.lower(name), "rightchat", 1, true) ~= nil
+end
+
+local function TouchesRightChatPanel(value, seen, depth)
+  if not value or depth > 8 then return false end
+  seen = seen or {}
+  if seen[value] then return false end
+  seen[value] = true
+
+  if IsRightChatNamed(value)
+    or value == _G.RightChatPanel
+    or value == _G.RightChatDataPanel
+    or value == _G.RightChatTab then
+    return true
+  end
+
+  if type(value.GetParent) == "function" then
+    local ok, parent = pcall(value.GetParent, value)
+    if ok and parent and TouchesRightChatPanel(parent, seen, depth + 1) then return true end
+  end
+
+  if type(value.GetNumPoints) == "function" and type(value.GetPoint) == "function" then
+    local ok, count = pcall(value.GetNumPoints, value)
+    if ok then
+      for pointIndex = 1, tonumber(count) or 0 do
+        local pointOK, _, relativeTo = pcall(value.GetPoint, value, pointIndex)
+        if pointOK and relativeTo and TouchesRightChatPanel(relativeTo, seen, depth + 1) then return true end
+      end
+    end
+  end
+
+  return false
+end
+
+local function IsChatFrameInRightPanel(frame)
+  if not frame then return false end
+  if TouchesRightChatPanel(frame, {}, 0) then return true end
+
+  local frameName = ObjectName(frame)
+  if frameName then
+    for _, suffix in ipairs({"Tab", "ButtonFrame", "EditBox"}) do
+      local companion = _G[frameName .. suffix]
+      if companion and TouchesRightChatPanel(companion, {}, 0) then return true end
+    end
+  end
+
+  return false
+end
+
 function RUI:RemoveRightLootTradeChat()
   local removed = false
 
-  -- Close custom chat windows named Loot/Trade (and common variants).
+  -- Close standalone Loot/Trade windows only. Any chat frame or tab attached
+  -- to ElvUI's right chat panel is user-managed content and must be preserved.
   if type(NUM_CHAT_WINDOWS) == "number" and type(GetChatWindowInfo) == "function" then
     for index = NUM_CHAT_WINDOWS, 1, -1 do
       local name = GetChatWindowInfo(index)
       local lower = type(name) == "string" and string.lower(name) or ""
       if lower ~= "" and (string.find(lower, "loot", 1, true) or string.find(lower, "trade", 1, true)) then
         local frame = _G["ChatFrame" .. index]
-        if frame then
+        if frame and not IsChatFrameInRightPanel(frame) then
           if type(FCF_UnDockFrame) == "function" then pcall(FCF_UnDockFrame, frame) end
           if type(FCF_Close) == "function" then
             pcall(FCF_Close, frame)
@@ -134,10 +192,8 @@ function RUI:RemoveRightLootTradeChat()
     end
   end
 
-  -- Keep ElvUI's right chat container and its data-text panel intact. Older
-  -- RetreatUI builds hid these frames after every login, which made the chat
-  -- tab disappear and left the data texts invisible while their tooltips still
-  -- reacted to the mouse. Only the unwanted Loot/Trade chat windows are closed.
+  -- The right chat container, its data-text panel, and every chat tab placed
+  -- inside it remain untouched.
   return removed
 end
 
@@ -152,7 +208,7 @@ local TARGET_TARGET_MOVER_FALLBACK = "BOTTOM,ElvUIParent,BOTTOM,508,350"
 local TARGET_TARGET_GAP = 8
 local TARGET_TARGET_WIDTH = 120
 local TARGET_TARGET_HEIGHT = 24
-local HUD_POLISH_REVISION = 2
+local HUD_POLISH_REVISION = 3
 local PLAYER_CASTBAR_MOVER = "BOTTOM,ElvUIParent,BOTTOM,-310,326"
 local TARGET_CASTBAR_MOVER = "BOTTOM,ElvUIParent,BOTTOM,310,326"
 local ACTIONBAR3_MOVER = "BOTTOM,ElvUIParent,BOTTOM,0,26"
@@ -162,6 +218,31 @@ local function NumberOr(value, fallback)
   return type(value) == "number" and value or fallback
 end
 
+local function IsLegacyKnightFontColor(value)
+  if type(value) ~= "table" then return true end
+  local red = tonumber(value.r)
+  local green = tonumber(value.g) or 0
+  local blue = tonumber(value.b) or 0
+  if red == nil then return green <= 0.08 and blue <= 0.08 end
+  return red >= 0.88 and green <= 0.10 and blue <= 0.10
+end
+
+local function ApplyClassFontColor(profile, force)
+  if type(profile) ~= "table" then return false end
+  profile.general = profile.general or {}
+  if not force and not IsLegacyKnightFontColor(profile.general.valuecolor) then return false end
+
+  local theme = RUI:GetTheme()
+  local color = theme and theme.accent or {1, 1, 1}
+  profile.general.valuecolor = {
+    r = NumberOr(color[1], 1),
+    g = NumberOr(color[2], 1),
+    b = NumberOr(color[3], 1),
+    a = 1,
+  }
+  return true
+end
+
 local function BuildPartyMoverPosition(E)
   local parent = _G.ElvUIParent or UIParent
   local parentWidth = parent and parent.GetWidth and parent:GetWidth() or 1920
@@ -411,14 +492,20 @@ function RUI:ApplyElvUIHUDPolish(force)
   local applyMovers = force or db.integrations.elvui.hudPolishRevision ~= HUD_POLISH_REVISION
 
   ConfigureHUDPolish(E.db, applyMovers)
+  local classFontColorApplied = ApplyClassFontColor(E.db, false)
   if ElvDB.profiles and ElvDB.profiles.RetreatUI then
     ConfigureHUDPolish(ElvDB.profiles.RetreatUI, applyMovers)
+    classFontColorApplied = ApplyClassFontColor(ElvDB.profiles.RetreatUI, false) or classFontColorApplied
   end
 
   if applyMovers then
     db.integrations.elvui.hudPolishRevision = HUD_POLISH_REVISION
     db.integrations.elvui.hudPolishVersion = self.version
   end
+  if classFontColorApplied then
+    db.integrations.elvui.classFontColorVersion = self.version
+    db.integrations.elvui.classFontColorClass = type(self.GetDetectedClass) == "function" and self:GetDetectedClass() or nil
+  end
 
   if E.UpdateMoverPositions then pcall(E.UpdateMoverPositions, E) end
   if E.UpdateAll then pcall(E.UpdateAll, E, true) end
@@ -500,6 +587,7 @@ function RUI:InstallElvUIProfile()
   ElvDB.profiles = ElvDB.profiles or {}
   local profile = self:DeepCopy(self.ElvUIProfile)
   self:ForceFontFields(profile)
+  ApplyClassFontColor(profile, true)
   ElvDB.profiles[profileName] = profile
 
   local ok, err = pcall(function()
 diff --git a/RetreatUI/Loader.lua b/RetreatUI/Loader.lua
index 547f9d5..865129b 100644
--- a/RetreatUI/Loader.lua
+++ b/RetreatUI/Loader.lua
@@ -2,7 +2,7 @@ RetreatUI = RetreatUI or {}
 local RUI = RetreatUI
 
 RUI.name = "RetreatUI"
-RUI.version = (GetAddOnMetadata and GetAddOnMetadata("RetreatUI", "Version")) or "1.0.11-beta.4"
+RUI.version = (GetAddOnMetadata and GetAddOnMetadata("RetreatUI", "Version")) or "1.0.11-beta.5"
 RUI._loaderLoaded = true
 
 local function Chat(message)
 diff --git a/RetreatUI/Profiles/ElvUI.lua b/RetreatUI/Profiles/ElvUI.lua
index b43eec3..34b3e7e 100644
--- a/RetreatUI/Profiles/ElvUI.lua
+++ b/RetreatUI/Profiles/ElvUI.lua
@@ -119,9 +119,12 @@ RUI.ElvUIProfile = {
       size = 36,
       spacing = 2,
     },
+    -- Replaced with the active RetreatUI class accent during installation.
     valuecolor = {
-      b = 0.02,
-      g = 0,
+      a = 1,
+      b = 1,
+      g = 1,
+      r = 1,
     },
     watchFrameHeight = 360,
   },
 diff --git a/RetreatUI/README.txt b/RetreatUI/README.txt
index f7ec208..50cef2d 100644
--- a/RetreatUI/README.txt
+++ b/RetreatUI/README.txt
@@ -1,4 +1,4 @@
-RETREATUI v1.0.11-beta.4
+RETREATUI v1.0.11-beta.5
 ===================
 
 RetreatUI is a compact Conquest of Azeroth interface package built around ElvUI, TurboPlates, Details! and class-specific combat HUDs.
 diff --git a/RetreatUI/RetreatUI.toc b/RetreatUI/RetreatUI.toc
index 0ce52b2..ab9cae9 100644
--- a/RetreatUI/RetreatUI.toc
+++ b/RetreatUI/RetreatUI.toc
@@ -2,7 +2,7 @@
 ## Title: RetreatUI
 ## Notes: Core interface, installer, profiles and shared HUD engine for Project Ascension: Conquest of Azeroth.
 ## Author: Retreat
-## Version: 1.0.11-beta.4
+## Version: 1.0.11-beta.5
 ## SavedVariables: RetreatUIDB
 ## OptionalDeps: ElvUI, TurboPlates, MobSpells, Details, DBM-Core
 ## DefaultState: Enabled
 diff --git a/RetreatUI_Classes/Loader.lua b/RetreatUI_Classes/Loader.lua
index 1fcf174..5d17549 100644
--- a/RetreatUI_Classes/Loader.lua
+++ b/RetreatUI_Classes/Loader.lua
@@ -3,7 +3,7 @@ local RUI = RetreatUI
 if not RUI then return end
 
 RUI.classesAddonName = ADDON_NAME or "RetreatUI_Classes"
-RUI.classesVersion = (GetAddOnMetadata and GetAddOnMetadata(RUI.classesAddonName, "Version")) or "1.0.11-beta.4"
+RUI.classesVersion = (GetAddOnMetadata and GetAddOnMetadata(RUI.classesAddonName, "Version")) or "1.0.11-beta.5"
 RUI.classesLoaded = true
 RUI.classPackage = RUI.classPackage or {}
 RUI.classPackage.name = RUI.classesAddonName
 diff --git a/RetreatUI_Classes/README.txt b/RetreatUI_Classes/README.txt
index 50867de..01b0d6a 100644
--- a/RetreatUI_Classes/README.txt
+++ b/RetreatUI_Classes/README.txt
@@ -1,4 +1,4 @@
-RETREATUI CLASSES v1.0.11-beta.4
+RETREATUI CLASSES v1.0.11-beta.5
 ==========================
 
 This addon is bundled with RetreatUI and must always use the same version as Core.
 diff --git a/RetreatUI_Classes/RetreatUI_Classes.toc b/RetreatUI_Classes/RetreatUI_Classes.toc
index 399f3ab..7c69bbf 100644
--- a/RetreatUI_Classes/RetreatUI_Classes.toc
+++ b/RetreatUI_Classes/RetreatUI_Classes.toc
@@ -2,7 +2,7 @@
 ## Title: RetreatUI Classes
 ## Notes: Class metadata, spell data and class-specific HUD modules for RetreatUI.
 ## Author: Retreat
-## Version: 1.0.11-beta.4
+## Version: 1.0.11-beta.5
 ## Dependencies: RetreatUI
 ## DefaultState: Enabled
 
