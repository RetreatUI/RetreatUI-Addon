--- a/RetreatUI_Classes/AdvancedHUD.lua
+++ b/RetreatUI_Classes/AdvancedHUD.lua
@@ -946,12 +946,19 @@
       local resourceLayout = RUI.layout.demonfire or {x=0, y=-118}
       local x = tonumber(resourceLayout.x) or 0
       local y = tonumber(resourceLayout.y) or -118
-      state.root.resourceLabel:ClearAllPoints()
-      state.root.resourceLabel:SetPoint("CENTER", UIParent, "CENTER", x, y + 22 * scale)
-      if state.root.resourceLabel.SetScale and RUI.GetHUDScale then state.root.resourceLabel:SetScale(RUI:GetHUDScale("demonfire")) end
-      state.root.resourceLabel:SetText(label .. "  " .. tostring(current) .. " / " .. tostring(maximum))
-      state.root.resourceLabel:SetTextColor(theme.accent[1],theme.accent[2],theme.accent[3],1)
-      state.root.resourceLabel:Show()
+      if config.showLabel == false or label == "" then
+        state.root.resourceLabel:SetText("")
+        state.root.resourceLabel:SetAlpha(0)
+        state.root.resourceLabel:Hide()
+      else
+        state.root.resourceLabel:SetAlpha(1)
+        state.root.resourceLabel:ClearAllPoints()
+        state.root.resourceLabel:SetPoint("CENTER", UIParent, "CENTER", x, y + 22 * scale)
+        if state.root.resourceLabel.SetScale and RUI.GetHUDScale then state.root.resourceLabel:SetScale(RUI:GetHUDScale("demonfire")) end
+        state.root.resourceLabel:SetText(label .. "  " .. tostring(current) .. " / " .. tostring(maximum))
+        state.root.resourceLabel:SetTextColor(theme.accent[1],theme.accent[2],theme.accent[3],1)
+        state.root.resourceLabel:Show()
+      end
       for index, segment in ipairs(state.resourceSegments) do
         if index <= maximum then
           local active = index <= current
--- a/RetreatUI_Classes/Runemaster/Data.lua
+++ b/RetreatUI_Classes/Runemaster/Data.lua
@@ -7,7 +7,7 @@
   disableLiveClassCooldowns = true,
   tabs = {"Arcane","Riftblade","Runic"},
   resources = {{key="primary", name="Primary Power", type="primary", position="power"}},
-  nativeResource = {title="INSCRIBED RUNES",keywords={"runic tattoo","etching of the leylines","harnessed leylines","harnessing leylines","power overwhelming","runic"},auraNames={"Runic Tattoo","Etching of the Leylines","Harnessed Leylines","Harnessing Leylines","Power Overwhelming"},spellIDs={803783,801106},maxByName={["runic tattoo"]=4,["etching of the leylines"]=4,["harnessed leylines"]=10,["harnessing leylines"]=10,["power overwhelming"]=1},maximum=4,defaultCurrent=0,keepVisible=true,mode="segments",icon="Interface\\Icons\\Spell_Arcane_Rune"},
+  nativeResource = {title="INSCRIBED RUNES",keywords={"runic tattoo","etching of the leylines","harnessed leylines","harnessing leylines","power overwhelming","runic"},auraNames={"Runic Tattoo","Etching of the Leylines","Harnessed Leylines","Harnessing Leylines","Power Overwhelming"},spellIDs={803783,801106},maxByName={["runic tattoo"]=4,["etching of the leylines"]=4,["harnessed leylines"]=10,["harnessing leylines"]=10,["power overwhelming"]=1},maximum=4,defaultCurrent=0,keepVisible=true,mode="segments",showLabel=false,icon="Interface\\Icons\\Spell_Arcane_Rune"},
   spells = {
     {name="Smolder", id=801087, category="rotation", hudRow="core", order=16, trackCooldown=true, glowWhenAura={"Windsage","Spellfire Runes"}, talent=true, fallbackIcon="Interface\\icons\\uico_fire_spell_02_Border", sourceTab="Riftblade", collectorEntryID=31212, cooldownHint=6},
     {name="Hoarfrost", id=801104, category="rotation", hudRow="core", order=20, trackCooldown=true, targetDebuff=true, glowWhenAura={"Leyfrost","Runic Invocation"}, talent=true, fallbackIcon="Interface\\icons\\nhi_iceempower_Border", sourceTab="Riftblade", collectorEntryID=30026, cooldownHint=10},
--- a/RetreatUI_Classes/Runemaster/HUD.lua
+++ b/RetreatUI_Classes/Runemaster/HUD.lua
@@ -5,7 +5,7 @@
 -- secondary utility row. RetreatUI keeps that structure while using learned
 -- spell detection, native cooldowns, build profiles and active-only proc icons.
 -- The source WeakAura remains external and is never imported or modified.
-local module = RUI:RegisterAdvancedClassHUD("Runemaster", {
+RUI:RegisterAdvancedClassHUD("Runemaster", {
   frameName = "RetreatUIRunemasterHUD",
   usesPrimaryPower = true,
   supportedLoadouts = {ARCANE=true,RIFTBLADE=true,RUNIC=true},
@@ -43,33 +43,3 @@
 
   maxProcs = 10,
 })
-
--- Inscribed Runes is intentionally icon-only. The shared resource renderer
--- also owns labelled resources for other classes, so suppress only the
--- Runemaster label after its native HUD frame has been created.
-local function HideInscribedRunesLabel()
-  local root = _G.RetreatUIRunemasterHUD
-  if root and root.resourceLabel then
-    root.resourceLabel:SetText("")
-    root.resourceLabel:SetAlpha(0)
-    root.resourceLabel:Hide()
-  end
-end
-
-if module then
-  local originalActivate = module.activate
-  function module:activate(...)
-    local result
-    if originalActivate then result = originalActivate(self, ...) end
-    HideInscribedRunesLabel()
-    return result
-  end
-
-  local originalRefreshLayout = module.refreshLayout
-  function module:refreshLayout(...)
-    local result
-    if originalRefreshLayout then result = originalRefreshLayout(self, ...) end
-    HideInscribedRunesLabel()
-    return result
-  end
-end
--- a/RetreatUI/Core/TrinketTracker.lua
+++ b/RetreatUI/Core/TrinketTracker.lua
@@ -63,7 +63,7 @@
     _G.PlayerFrame,
   }
   for _, frame in ipairs(candidates) do
-    if frame and frame.GetRight and frame.GetTop then return frame end
+    if frame and FrameBounds(frame) then return frame end
   end
   return nil
 end
@@ -155,9 +155,9 @@
 end
 
 local function FindTrinketAura(state, buffs)
-  if not state.itemID then return nil end
+  if not state.equipped then return nil end
 
-  local learned = LearnedAuraForItem(state.itemID)
+  local learned = state.itemID and LearnedAuraForItem(state.itemID) or nil
   if learned then
     local aura = learned.spellID and buffs.byID[Number(learned.spellID, nil)] or nil
     aura = aura or buffs.byName[Normalize(learned.name)]
@@ -225,7 +225,7 @@
     icon:SetPoint("LEFT", tracker, "LEFT", (index - 1) * (size + ICON_SPACING), 0)
     ConfigureTooltip(icon, slot)
     tracker.icons[index] = icon
-    states[index] = {slot=slot, icon=icon, itemID=nil, lastCooldownStart=0, lastCooldownDuration=0}
+    states[index] = {slot=slot, icon=icon, itemID=nil, itemLink=nil, texture=nil, equipped=false, lastCooldownStart=0, lastCooldownDuration=0}
   end
   tracker:Hide()
   return tracker
@@ -303,31 +303,40 @@
 local function RefreshItems(force)
   if not tracker then return end
   for index, state in ipairs(states) do
+    local link = GetInventoryItemLink and GetInventoryItemLink("player", state.slot) or nil
     local itemID = GetInventoryItemID and GetInventoryItemID("player", state.slot) or nil
-    if not itemID and GetInventoryItemLink then
-      local link = GetInventoryItemLink("player", state.slot)
-      itemID = link and tonumber(string.match(link, "item:(%d+)")) or nil
+    if not itemID and link then
+      itemID = tonumber(string.match(link, "item:(%d+)"))
     end
-    if force or itemID ~= state.itemID then
+    local texture = GetInventoryItemTexture and GetInventoryItemTexture("player", state.slot) or nil
+    -- Some Ascension client builds do not expose GetInventoryItemID reliably
+    -- for equipped custom items. A slot texture or item link still proves that
+    -- a trinket is equipped and is enough for slot-based cooldown tracking.
+    local equipped = itemID ~= nil or link ~= nil or texture ~= nil
+
+    if force or itemID ~= state.itemID or link ~= state.itemLink
+      or texture ~= state.texture or equipped ~= state.equipped then
       state.itemID = itemID
+      state.itemLink = link
+      state.texture = texture
+      state.equipped = equipped
       state.activationUntil = nil
       state.lastCooldownStart = 0
       state.lastCooldownDuration = 0
       state.itemSpellName, state.itemSpellID, state.itemSpellTexture = ItemSpell(itemID)
-      state.texture = GetInventoryItemTexture and GetInventoryItemTexture("player", state.slot) or nil
-      state.icon.texture:SetTexture(state.texture or "Interface\\Icons\\INV_Misc_QuestionMark")
+      state.icon.texture:SetTexture(texture or "Interface\\Icons\\INV_Misc_QuestionMark")
       state.icon.stackText:SetText("")
       state.icon.cooldownText:SetText("")
       W:SetGlow(state.icon, nil, 0)
       W:SetBorder(state.icon, {0,0,0,1}, 1)
-      if itemID then state.icon:Show() else state.icon:Hide() end
+      if equipped then state.icon:Show() else state.icon:Hide() end
     end
   end
 end
 
 local function UpdateState(state, buffs, now)
   local icon = state.icon
-  if not state.itemID then icon:Hide(); return false end
+  if not state.equipped then icon:Hide(); return false end
   icon:Show()
 
   local start, duration, enabled = ReadCooldown(state.slot)
--- a/RetreatUI/Core/Version.lua
+++ b/RetreatUI/Core/Version.lua
@@ -2,12 +2,13 @@
 
 RUI.changelog = {
   version = RUI.version,
-  title = "RetreatUI v1.1.0-beta.18",
-  summary = "Runemaster localization and Inscribed Runes display hotfix.",
+  title = "RetreatUI v1.1.0-beta.19",
+  summary = "Inscribed Runes and trinket HUD reliability hotfix.",
   changes = {
-    "Made Runemaster detection language-independent for official localized Ascension clients.",
-    "Added SPIRITMAGE class-token aliases and spell-ID fallbacks for Chinese and other localized clients.",
-    "Removed the Inscribed Runes title and 0/4 counter; the resource now displays only four rune icons.",
+    "Fixed the shared segmented-resource renderer recreating the Inscribed Runes title and 0/4 counter.",
+    "Runemaster now permanently displays only four centered rune icons.",
+    "Made the trinket HUD recognize equipped Ascension trinkets even when the client does not return a standard item ID.",
+    "Improved trinket HUD player-frame anchoring by skipping invalid or hidden frame candidates.",
   },
 }
 
--- a/RetreatUI/Core/CompatibilityChangelog.lua
+++ b/RetreatUI/Core/CompatibilityChangelog.lua
@@ -2,11 +2,12 @@
 
 RUI.changelog = {
   version = RUI.version,
-  title = "RetreatUI v1.1.0-beta.18",
-  summary = "Runemaster localization and Inscribed Runes display hotfix.",
+  title = "RetreatUI v1.1.0-beta.19",
+  summary = "Inscribed Runes and trinket HUD reliability hotfix.",
   changes = {
-    "Made Runemaster detection language-independent by recognizing the SPIRITMAGE token and core spell IDs.",
-    "Fixed Runemaster being reported as unsupported on the official Chinese Ascension client.",
-    "Changed Inscribed Runes to four clean rune icons with no title or numeric counter.",
+    "Fixed the shared segmented-resource renderer recreating the Inscribed Runes title and 0/4 counter.",
+    "Runemaster now permanently displays only four centered rune icons.",
+    "Made the trinket HUD recognize equipped Ascension trinkets even when the client does not return a standard item ID.",
+    "Improved trinket HUD player-frame anchoring by skipping invalid or hidden frame candidates.",
   },
 }
--- a/RetreatUI/RetreatUI.toc
+++ b/RetreatUI/RetreatUI.toc
@@ -2,7 +2,7 @@
 ## Title: RetreatUI
 ## Notes: Core interface, installer, profiles and shared HUD engine for Project Ascension: Conquest of Azeroth.
 ## Author: Retreat
-## Version: 1.1.0-beta.18
+## Version: 1.1.0-beta.19
 ## SavedVariables: RetreatUIDB, RetreatUIAutomationDB
 ## OptionalDeps: ElvUI, TurboPlates, MobSpells, Details, DBM-Core
 ## DefaultState: Enabled
--- a/RetreatUI_Classes/RetreatUI_Classes.toc
+++ b/RetreatUI_Classes/RetreatUI_Classes.toc
@@ -2,7 +2,7 @@
 ## Title: RetreatUI Classes
 ## Notes: Class metadata, spell data and class-specific HUD modules for RetreatUI.
 ## Author: Retreat
-## Version: 1.1.0-beta.18
+## Version: 1.1.0-beta.19
 ## Dependencies: RetreatUI
 ## DefaultState: Enabled
 