local RUI = RetreatUI
if not RUI then return end

-- Generated at build/release time by tools/generate_beta20_weakauras.py.
-- Kept as a valid source-tree placeholder so the addon can still load from a
-- raw checkout; packaged beta.20 builds replace this registry with the finished
-- General + 21 CoA !WA:2! payload strings.
RUI.NaowhCoAWeakAuras = RUI.NaowhCoAWeakAuras or {classes = {}}
RUI.NaowhCoAWeakAuras.classes = RUI.NaowhCoAWeakAuras.classes or {}
RUI.NaowhCoAWeakAuras.generatedFor = "source-placeholder"
